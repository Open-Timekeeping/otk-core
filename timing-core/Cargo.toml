[package]
name        = "timing-core"
version     = "0.1.0"
edition     = "2021"
license     = "Apache-2.0"
description = "Timing-domain engine for OTK. Converts raw detections into crossings."

[dependencies]
event-model = { workspace = true }
uuid        = { version = "1", features = ["v4"] }
